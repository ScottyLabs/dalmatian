{
  "name": "dalmatian",
  "version": "0.0.1",
  "description": "test for new cmu bot",
  "main": "./build/index.js",
  "scripts": {
    "build": "rm -rf ./build && tsc",
    "start": "pnpm run start:bot",
    "start:bot": "pnpm run build && node --env-file=.env ./build/index.js"
  },
  "author": "ScottyLabs",
  "license": "",
  "dependencies": {
    "discord.js": "^14.22.1",
    "dotenv": "^17.2.2",
    "fast-fuzzy": "^1.12.0"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}
